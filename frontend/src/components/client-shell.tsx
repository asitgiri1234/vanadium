"use client";

import { ScrollProgress } from "@/components/scroll-progress";

/** Client-only shell widgets used by the root layout. */
export function ClientShell() {
  return <ScrollProgress />;
}
