# Vercel production — use either (API_URL is read at runtime, no rebuild needed):
API_URL=https://vanadium-1.onrender.com
# NEXT_PUBLIC_API_URL=https://vanadium-1.onrender.com

# YouTube Data API v3 — used by /api/youtube/metadata proxy (duration, subscribers)
YOUTUBE_API_KEY=
