# --- Web framework ---
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.1
python-multipart>=0.0.9

# --- Config & validation ---
pydantic>=2.6
pydantic-settings>=2.2

# --- AI orchestration ---
langchain>=0.2
langchain-openai>=0.1
langchain-community>=0.2
openai>=1.30

# --- Vector store ---
chromadb>=0.5
numpy>=2.0,<2.5

# --- Transcript / metadata extraction ---
youtube-transcript-api>=0.6.2
yt-dlp>=2024.4.9

# --- HTTP / utils ---
httpx>=0.27
tenacity>=8.2

# --- Instagram Reels transcription ---
# Whisper transcribes reel audio (set ENABLE_WHISPER=true). It pulls in PyTorch
# (large) and requires the ffmpeg binary to be installed and on PATH.
openai-whisper>=20231117

# --- Visual understanding (set ENABLE_VISUAL=true) ---
# OCR reads on-screen text from sampled frames. Requires the Tesseract binary
# (install separately; point TESSERACT_CMD at it on Windows). The optional
# scene description uses the configured vision LLM when an OpenAI key is set.
pytesseract>=0.3.10
pillow>=10.0
